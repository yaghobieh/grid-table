# Screenshots for README

Add these images so the main README displays them:

- **full-table.png** — Full grid table (desktop, with toolbar and pagination)
- **studio-panel.png** — Table with Studio side panel open (data / props / generate tabs)
- **mobile-view.png** — Responsive mobile card view

Capture from the demo app (`npm run dev` in the repo root, then run the demo).
